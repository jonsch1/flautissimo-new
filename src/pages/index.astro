---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const allBlogPosts = await getCollection('blog');
const recentPosts = allBlogPosts
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);
---

<BaseLayout>
  <!-- Hero Section -->
  <div
    class="relative bg-cover bg-center bg-fixed h-[300px] md:h-[400px] flex items-center"
    style="background-image: url('/img/startseite-option.JPG')"
  >
    <div class="container mx-auto px-4">
      <div class="max-w-2xl">
        <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-4 bg-primary px-4 py-2 inline-block">
          Der Musikladen in Aachen
        </h1>
        <h3 class="text-lg md:text-xl lg:text-2xl font-semibold text-white bg-primary px-4 py-2 inline-block">
          Hier können Sie Flöten, Gitarren und vieles mehr kaufen..
        </h3>
      </div>
    </div>
  </div>

  <!-- YouTube Video -->
  <div class="relative w-[85%] mx-auto my-8 pb-[56.25%]">
    <iframe
      class="absolute top-0 left-0 w-full h-full"
      src="https://www.youtube-nocookie.com/embed/5n4SkVQkfEw"
      frameborder="0"
      allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
      allowfullscreen
      title="Musikhaus Aachen Vorstellung"
    ></iframe>
  </div>

  <!-- Main Content -->
  <section class="py-12 bg-gradient-to-b from-gray-50 to-white">
    <div class="container mx-auto px-4 max-w-5xl">
      <div class="mb-12">
        <h2 class="text-3xl md:text-4xl font-semibold mb-6">Warum Musikhaus Aachen?</h2>

        <p class="text-lg text-gray-700 mb-4">
          Musikhaus-Aachen ist das Musikgeschäft in Aachen mit einem sehr umfangreichen Sortiment in
          den Bereichen <strong>Blockflöte, Gitarre, Ukulele, Musiknoten und vielem Weiteren.</strong>
        </p>

        <p class="text-lg text-gray-700 mb-8">
          Für einen Eindruck von unserem Sortiment schauen Sie gerne in das Vorstellungsvideo.
        </p>

        <h3 class="text-2xl md:text-3xl font-semibold mb-6">Musikgeschäft im Herzen Burtscheids</h3>

        <ul class="space-y-4 text-lg text-gray-700">
          <li class="flex items-start">
            <span class="text-primary mr-2">•</span>
            <span><strong>Bei uns können Sie in entspannter, familienfreundlicher Atmosphäre Musikinstrumente ausprobieren.</strong></span>
          </li>
          <li class="flex items-start">
            <span class="text-primary mr-2">•</span>
            <span><strong>Falls Sie sich für ein Instrument entscheiden, können Sie es zu einem fairen Preis erwerben.</strong></span>
          </li>
          <li class="flex items-start">
            <span class="text-primary mr-2">•</span>
            <span><strong>Sie profitieren darüberhinaus von unserem kulanten Service falls es mal zu einem Problem kommen sollte.</strong></span>
          </li>
        </ul>
      </div>

      <!-- CTA Button -->
      <div class="text-center mb-12">
        <a
          href="/products"
          class="inline-block bg-primary hover:bg-primary-dark text-white font-bold py-3 px-8 rounded-lg transition-colors"
        >
          Zum Sortiment
        </a>
      </div>

      <!-- Recent Blog Posts -->
      <div class="mb-8">
        <h3 class="text-2xl md:text-3xl font-semibold mb-8">Die neusten Geschichten</h3>

        <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
          {recentPosts.map((post) => (
            <article class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow">
              <div class="p-6">
                <h4 class="text-xl font-semibold mb-2">
                  <a href={`/blog/${post.slug}`} class="text-gray-900 hover:text-primary">
                    {post.data.title}
                  </a>
                </h4>
                <p class="text-sm text-gray-500 mb-3">
                  {post.data.date.toLocaleDateString('de-DE', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric'
                  })}
                </p>
                <p class="text-gray-700 mb-4">{post.data.description}</p>
                <a
                  href={`/blog/${post.slug}`}
                  class="text-primary hover:text-primary-dark font-medium"
                >
                  Weiterlesen →
                </a>
              </div>
            </article>
          ))}
        </div>

        <div class="text-center mt-8">
          <a
            href="/blog"
            class="inline-block bg-primary hover:bg-primary-dark text-white font-bold py-3 px-8 rounded-lg transition-colors"
          >
            Mehr lesen
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
